<div class="card">
  <div class="page-title">
    <h1>모든 포스트</h1>
    <%= link_to "새 포스트 작성", new_post_path, class: "btn btn-primary" %>
  </div>

  <% if @posts.any? %>
    <div class="posts-grid">
      <% @posts.each do |post| %>
        <div class="post-card">
          <h2 class="post-title">
            <%= link_to post.title, post_path(post) %>
          </h2>
          
          <p class="post-summary"><%= post.summary %></p>
          
          <div class="post-actions">
            <%= link_to "보기", post_path(post), class: "view" %>
            <%= link_to "수정", edit_post_path(post), class: "edit" %>
            <%= button_to "삭제", post_path(post), 
                method: :delete, 
                data: { turbo_confirm: "정말로 이 포스트를 삭제하시겠습니까?" },
                class: "delete",
                form: { style: 'display: inline;' } %>
          </div>
          
          <div class="post-meta">
            <span>작성자: <%= post.author %></span>
            <span>작성일: <%= post.created_at.strftime("%Y년 %m월 %d일 %H:%M") %></span>
          </div>
        </div>
      <% end %>
    </div>
  <% else %>
    <div class="empty-state">
      <h3>아직 포스트가 없습니다</h3>
      <p>첫 번째 포스트를 작성해보세요!</p>
      <%= link_to "포스트 작성하기", new_post_path, class: "btn btn-primary" %>
    </div>
  <% end %>

  <div class="mt-3">
    <%= link_to "홈으로 돌아가기", root_path, class: "btn btn-secondary" %>
  </div>
</div> 